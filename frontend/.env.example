# Frontend environment
# Copy to .env for local overrides.

# Production only — set on Vercel (include /api suffix):
# VITE_API_URL=https://your-render-api.onrender.com/api

# Local development: leave VITE_API_URL unset to use Vite proxy (/api → http://127.0.0.1:8000)
